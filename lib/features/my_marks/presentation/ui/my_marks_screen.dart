import 'package:flutter/material.dart';

class MyMarksScreen extends StatelessWidget {
  const MyMarksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("My Marks Screen"),
    );
  }
}
